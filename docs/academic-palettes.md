# 内置学术科研配色

- 来源：[颜色代码表 · 学术科研](https://www.ysdaima.com/palettes/academic/)，核对日期 2026-09-08，共 260 套。
- 范围：网站标记为 academic 的全部条目，只保存 ID、名称、标签和 HEX 颜色顺序，保留来源链接。
- 交互：图形属性 → 曲线 → 配色；搜索名称/标签，选择方案或点击色块；沿用预览、应用、取消语义。
- 当前属性编辑器仅编辑第一条曲线。选色同步线条与标记边框，已有无填充状态保留。其他线型、尺寸、坐标和数据绑定保持原值。
- 沿用 theme.palette、lineStyle、markerStyle 保存颜色，不修改共享 schema、依赖、默认主题和项目文件版本。
- 网站存在同名和同色方案，以 ID 区分选项；打开项目按颜色数组匹配名称，完全同色方案显示目录中的首个匹配项。
- 保留原始配色，不保证每套配色都适合白底或具有色盲可读性。

执行与验证：

- [x] 核对属性编辑器与网站静态数据。
- [x] 先添加并运行行为测试，确认新功能缺失：4 个 UI 测试因配色标签不存在而失败，1 个属性测试因未更新 theme.palette 而失败。
- [x] 实现离线目录、搜索和换色。
- [x] 验证数据完整性、草稿取消、保存恢复、SVG 颜色、类型检查、构建和浏览器交互。

文档路径说明：会话指定的 `C:/Users/Obsidian/Codex` 不存在，尝试创建目录失败；本次沿用仓库现有 docs 目录保存功能及来源说明。

数据维护：

- 数据模块：`packages/web-editor/src/palettes/academic-palettes.ts`，单条来源记录占一行，使用局部 prettier-ignore 保持可核对的行结构。
- 网站本次引用的数据资源：`https://www.ysdaima.com/_astro/index.DOjdTaff.js`。
- 原始资源 SHA-256：`E87314EB77B4D6A146F4829E7832B3D31184478DD0E69AAABC2DC159B3AE236F`。
- 提取后仅统一 HEX 大小写；名称、标签和颜色顺序保留原值。未复制网站代码、配图、介绍文案或热度数据。
- 更新时从分类页确认最新数据资源，只读取 academic 条目，检查 ID 唯一、数量和 HEX 合法性，并运行配色数据与 UI 测试。应用运行时不联网同步。

验收结果（2026-09-08）：

- 逐条比对网站与本地的 ID、名称、标签和颜色顺序：260 条一致。
- `pnpm exec vitest run --maxWorkers=4 --reporter=dot`：57 个文件，357 项测试通过。
- 首次全量测试与构建同时运行，原有 Origin 安全边界用例触发 5000 ms 超时；该测试文件单独复测 21 项通过，限制测试并发后全量通过，没有修改测试超时或根配置。
- `pnpm typecheck`、`pnpm build`、本次涉及文件的 Prettier 检查及 `git diff --check` 通过。
- 浏览器验证：搜索 Nature、键盘选择第二色、应用后导入 xy.csv、重新打开属性弹窗，主图与草稿均显示对应颜色。
- 人工审查：沿用弹窗取消/应用行为；色板复制后更新，不修改静态目录；支持无填充、同色条目选择和搜索无结果；未改变 schema、依赖、默认主题或数据绑定。
- 同色方案不额外保存来源 ID，项目恢复时采用目录首个颜色匹配项；颜色顺序和曲线实际颜色完整恢复。
