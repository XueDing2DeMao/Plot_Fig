# Origin 风格属性分类实施计划

目标：依据用户提供的三张 Origin 截图，将已有属性弹窗改为对象导航、分类标签和实时预览。延续白底黑字，不要求人工验收。

范围：当前单图层首条曲线；图页背景/导出物理尺寸，图层位置/大小，曲线显示模式/线条/符号/图例，X/Y 轴显示/尺度与范围/数字标签/标题字体/轴线与主次刻度。现有 schema 可直接表达这些属性，不修改共享契约。渐变图层背景、堆叠、断轴、公式标签不伪装为已实现功能。

交互：选择对象切换标签；切换不丢草稿。默认曲线的线条标签。确定应用并关闭，应用提交但保留弹窗，取消仅丢弃自上次应用以来的修改。键盘可操作对象和标签。预览包含所有草稿修改。

- [x] 扩展 Web 本地属性适配：页、图层、曲线显示/图例、轴显示/尺度/字体/刻度，验证并保持项目往返。
- [x] 补齐 renderer 线型、六种标记形状与刻度/标签独立可见性，测试实际 SVG 输出。
- [x] 新增对象导航、标签栏和分类控件；保留实时预览与窄屏布局。
- [x] 测试确定/应用/取消、对象切换、属性实际生效，执行自动浏览器、全量测试、typecheck/build。

验证：`pnpm test` 53 文件 / 336 测试通过；`pnpm typecheck` 和 `pnpm build` 全工作区通过。无头 Chrome 验证分类切换、虚线、菱形标记、对数尺度、标签精度、应用保留弹窗、取消保留已应用结果、键盘标签导航及 390px 窄屏；无页面异常。实现复核覆盖草稿隔离、参数边界、项目往返和 SVG 输出。对数坐标跳过非正值，同时保持误差棒与源数据行的对应关系；次刻度数量限制为 0–100。
