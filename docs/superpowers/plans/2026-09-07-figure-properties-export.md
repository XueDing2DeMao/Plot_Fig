# 图形属性与导出实施计划

目标：增加坐标轴标题/范围、线条/标记样式和 SVG/PNG 下载。用户已要求直接实施，不安排人工验收。

方案：复用 FigureTemplate，仅编辑首个面板首条曲线和其绑定轴。表单草稿由“应用图形设置”一次提交，非法输入保留原图并显示错误。范围两项均留空为自动；仅填一项、非有限数、min >= max 或对数轴非正范围均拒绝。设置沿用项目序列化保存恢复。不修改公共 schema 或新增依赖。

导出：直接下载当前 SVG；PNG 用原生 Image/Canvas 按 viewBox 比例以 1/2/3 倍像素生成。无有效预览时禁用，导出中禁止重复，错误可见，object URL 在成功/失败路径释放。

- [x] state/figure-settings.ts 和测试：不可变更新、范围校验、绑定保持、项目往返。
- [x] FigurePropertiesPanel、AxisFields、SeriesFields 与 App 接线；组件测试应用/拒绝/恢复。
- [x] browser/figure-export.ts 与 ExportControls：下载内容、PNG 尺寸、失败和清理测试。
- [x] 定向与全量测试、类型检查、构建、本地代码复查、记录结果。

风险：非法草稿污染预览、项目恢复不同步、SVG 物理单位导致 PNG 失真、异步导出失败后控件锁定。测试覆盖上述路径。

文档位置：个人默认目录 C:/Users/Obsidian/Codex 不存在且无法创建，沿用本仓库现有计划目录。

## 完成记录

2026-09-07：51 个测试文件、320 项测试通过（本阶段增加 26 项）；Web Editor typecheck、Vite build、git diff --check 通过。使用已安装的 Playwright 运行独立无头 Chrome 自动检查，无需人工验收：编辑中文标题、固定范围、曲线颜色、标记形状，非法范围保留原图；下载 SVG 并读取文本，下载 PNG 并读取文件头确认 2000×1600；保存/打开项目恢复属性和列绑定；390px 视口无页面横向溢出，无 pageerror。

复查修正：将下拉框 label 与 select 显式关联，防止标签名称拼入全部选项；新属性保持在表单草稿中直到校验通过；PNG 解码失败也释放 URL，导出 busy 在 finally 恢复；文件下载 URL 延迟释放。

## 图形属性弹窗调整（2026-09-07）

用户要求将属性区域改为按钮和弹窗。已将入口移到主图预览标题栏，移除右侧常驻属性表单；原生 dialog 内左侧编辑、右侧实时预览，窄屏改为上方预览、下方滚动编辑。主图仅在应用时更新，取消/关闭/Esc 丢弃草稿，返回焦点到入口；无效输入显示原因并禁用应用，无数据时仍可配置并提示无法预览。

复用 SvgSurface 安全挂载主图和弹窗 SVG。51 files / 322 tests、类型检查、构建和差异检查通过。独立无头 Chrome 自动检查：实时预览与主图隔离、取消重开、Esc/焦点恢复、应用更新、非法范围与恢复自动范围、390px 弹窗及底部按钮不超出视口、无 pageerror；桌面和移动截图已检查。
